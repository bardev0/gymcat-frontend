import React from "react";

function UserHomePage() {
	// check if user is logged in on current machine, if not navigate to login box

	return (
	<>
	<div>
	<button>Logout</button>
	<h1>Login to enter!</h1></div>
	</>
	)
}

export default UserHomePage
